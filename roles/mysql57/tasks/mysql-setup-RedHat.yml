- name: Create group MySQL
  group:
    name: mysql
    state: present
  become: yes

- name: ADD MySQL user
  user:
    name: mysql
    shell: /bin/false
    group: mysql
    append: yes
  become: yes
